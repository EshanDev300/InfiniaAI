{
  "version": 2,
  "builds": [
    { "src": "New folder/server/server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "New folder/server/server.js" },
    { "src": "/(.*)", "dest": "New folder/$1" }
  ]
}
